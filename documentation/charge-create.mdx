---
title: "POST /v1/charges"
api: "POST https://api.debita.ai/v1/charges"
description: "Cria uma nova cobrança PIX ou Boleto com configurações personalizadas."
---

## Visão Geral

Cria uma nova cobrança que pode ser paga via PIX ou Boleto. O tipo de cobrança é determinado pelos parâmetros enviados.

## Parâmetros da Requisição

### Headers Obrigatórios

<ParamField header="Authorization" type="string" required>
  Chave API no formato Bearer token
  ```
  Bearer pk_sandbox_sua_chave
  ```
</ParamField>

<ParamField header="Content-Type" type="string">
  Tipo de conteúdo da requisição
  ```
  application/json
  ```
</ParamField>

### Body Parameters

<ParamField body="amount" type="integer" required>
  Valor da cobrança em centavos. R$ 100,00 = 10000
</ParamField>

<ParamField body="description" type="string" required>
  Descrição da cobrança (máximo 255 caracteres)
</ParamField>

<ParamField body="method" type="string" required>
  Método de pagamento: `pix`, `boleto`, ou `boleto_pix`
</ParamField>

<ParamField body="customer_id" type="string">
  ID do cliente existente (opcional)
</ParamField>

<ParamField body="customer" type="object">
  Dados do cliente (obrigatório se customer_id não fornecido)

  <Expandable title="Propriedades do customer">
    <ParamField body="customer.name" type="string" required>
      Nome completo do cliente
    </ParamField>
    <ParamField body="customer.document" type="string" required>
      CPF ou CNPJ do cliente
    </ParamField>
    <ParamField body="customer.email" type="string">
      Email do cliente
    </ParamField>
    <ParamField body="customer.phone" type="string">
      Telefone do cliente
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="due_date" type="string">
  Data de vencimento (obrigatório para boleto, formato YYYY-MM-DD)
</ParamField>

<ParamField body="expires_in" type="integer">
  Tempo de expiração em segundos (para PIX, padrão: 3600)
</ParamField>

<ParamField body="late_fine" type="object">
  Configuração de multa por atraso (para boleto)

  <Expandable title="Propriedades do late_fine">
    <ParamField body="late_fine.type" type="string" required>
      Tipo da multa: `Fixed` ou `Percentage`
    </ParamField>
    <ParamField body="late_fine.amount" type="number" required>
      Valor da multa (centavos ou percentual)
    </ParamField>
    <ParamField body="late_fine.date" type="string" required>
      Data de início da multa
    </ParamField>
  </Expandable>
</ParamField>

## Exemplo de Requisição

<RequestExample>

```bash PIX
curl -X POST https://api.debita.ai/v1/charges \
  -H "Authorization: Bearer pk_sandbox_sua_chave" \
  -H "Content-Type: application/json" \
  -d '{
    "amount": 10000,
    "description": "Pagamento PIX",
    "method": "pix",
    "customer": {
      "name": "João Silva",
      "document": "123.456.789-09",
      "email": "joao@email.com"
    },
    "expires_in": 3600
  }'
```

```bash Boleto
curl -X POST https://api.debita.ai/v1/charges \
  -H "Authorization: Bearer pk_sandbox_sua_chave" \
  -H "Content-Type: application/json" \
  -d '{
    "amount": 50000,
    "description": "Mensalidade Janeiro",
    "method": "boleto",
    "due_date": "2024-01-31",
    "customer": {
      "name": "Maria Santos",
      "document": "987.654.321-00"
    },
    "late_fine": {
      "type": "Percentage",
      "amount": 2.0,
      "date": "2024-02-05"
    }
  }'
```

```javascript Node.js
const response = await fetch('https://api.debita.ai/v1/charges', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer pk_sandbox_sua_chave',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    amount: 10000,
    description: 'Pagamento teste',
    method: 'pix',
    customer: {
      name: 'João Silva',
      document: '123.456.789-09',
      email: 'joao@email.com'
    }
  })
});

const charge = await response.json();
console.log('Cobrança criada:', charge.id);
```

</RequestExample>

## Resposta

<ResponseExample>

```json PIX (201)
{
  "id": "chg_pix_abc123",
  "status": "pending",
  "amount": 10000,
  "description": "Pagamento PIX",
  "method": "pix",
  "customer": {
    "id": "cust_def456",
    "name": "João Silva",
    "document": "123.456.789-09"
  },
  "pix": {
    "qr_code": "data:image/png;base64,iVBORw0KGgo...",
    "payload": "00020126580014br.gov.bcb.pix...",
    "expires_at": "2024-01-01T11:00:00-03:00"
  },
  "created_at": "2024-01-01T10:00:00-03:00"
}
```

```json Boleto (201)
{
  "id": "chg_boleto_xyz789",
  "status": "pending",
  "amount": 50000,
  "description": "Mensalidade Janeiro",
  "method": "boleto",
  "due_date": "2024-01-31",
  "customer": {
    "id": "cust_ghi789",
    "name": "Maria Santos",
    "document": "987.654.321-00"
  },
  "boleto": {
    "barcode": "34191234567890123456789012345678901234567890",
    "pdf_url": "https://api.debita.ai/boletos/chg_boleto_xyz789/pdf"
  },
  "late_fine": {
    "type": "Percentage",
    "amount": 2.0,
    "date": "2024-02-05"
  },
  "created_at": "2024-01-01T10:00:00-03:00"
}
```

</ResponseExample>